const INITIAL_STATE = {
  all: [
    { author: 'Janis', text: '🏰' },
    { author: 'Peter', text: '🗯' }
  ]
};

const messages = (state = INITIAL_STATE, action) => {
  return state;
};

export default messages;
